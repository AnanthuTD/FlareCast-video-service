// src/domain/enums/video/SearchVideosErrorType.ts
export enum SearchVideosErrorType {
  INVALID_INPUT = "Invalid input: query and workspaceId are required",
  INTERNAL_ERROR = "Failed to search videos",
}